// secrets.h.template — copy this file to secrets.h and fill in your values.
//
// secrets.h is in .gitignore and must NEVER be committed.
// For CI builds, set the GITHUB_TOKEN environment variable and use:
//   build_flags = -DGITHUB_TOKEN=\"${sysenv.GITHUB_TOKEN}\"
//
// The token only needs "issues:write" scope on the canvas-led-tracker repo.

#ifndef SECRETS_H
#define SECRETS_H

#define GITHUB_TOKEN "your_token_here"

#endif
